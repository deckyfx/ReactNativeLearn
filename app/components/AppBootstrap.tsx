import { useEffectOnce } from "@decky.fx/react-native-essentials";

const AppBootstrap = () => {
  const bootstrap = () => { 
    // load and save language to local realm
    // detect device used language and save language to local realm
  }

  useEffectOnce(bootstrap);

  return null;
};

export default AppBootstrap;
